server {
    listen 80;
    server_name _;

    root   /usr/share/nginx/html;
    index  index.html;

    location / {
        # Try the file, otherwise fall back to index.html (SPA routing)
        try_files $uri /index.html;
    }
}
